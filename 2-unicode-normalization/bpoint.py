c = input("code: ")

if c in ["breakpoint()"]: # normally there would be a lot of stuff here
    print("No cheating!")
else:
    eval(c)

# ᵇreakpoint()
# import unicodedata
# print(unicodedata.normalize("NFKD", "ᵇreakpoint()"))